# Auto detect text files and perform LF normalization
* text=auto

// Git LFS configuration for large files
Content/STF/** filter=lfs diff=lfs merge=lfs -text
Content/SecB2026/Maps/** filter=lfs diff=lfs merge=lfs -text